import { AdvanceHightlightDirective } from './advance-hightlight.directive';

describe('AdvanceHightlightDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvanceHightlightDirective();
    expect(directive).toBeTruthy();
  });
});
